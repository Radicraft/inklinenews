{
  "name": "inklinenews",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "icons": "node scripts/fetch-favicons.mjs",
    "dev": "astro dev",
    "gate:duplicates": "node scripts/gate-duplicates.mjs",
    "gate:feeds": "node scripts/gate-feed-urls.mjs",
    "build": "node scripts/fetch-news.mjs; node scripts/validate-registry.mjs && npm run gate:duplicates && astro build && npm run gate:feeds && node scripts/gate-colours.mjs && node scripts/gate-vocabulary.mjs && node scripts/build-sitemaps.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.5",
    "@fontsource-variable/literata": "^5.3.0",
    "@fontsource-variable/newsreader": "^5.3.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.117.0"
  }
}
